spring.datasource.url=jdbc:postgresql://localhost:5445/employee_db?stringtype=unspecified
spring.datasource.username=postgres
spring.datasource.password=kibuti@123

# Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.com.example.haba_backend=DEBUG

server.port=8082


spring.threads.virtual.enabled=true

#Actuator activation, here we need only enable required endpoints to be visible public
management.endpoints.web.exposure.include=health

spring.boot.message= hello employee service spring cloud bud demo
